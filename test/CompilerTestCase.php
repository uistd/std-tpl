<?php
function parseTag($tag_str)
{
    $len = strlen($tag_str);
    $type = 1;
    $tmp_str = '';
    for ($i = 0; $i < $len; ++$i) {
        $char = $tag_str[$i];
        $ord = ord($char);
        if (1 == $type) {
            
        }
    }
}

/**
 * 关键字解析
 * @param string $char
 * @param int $index 顺序
 */
function parseKey($char, $index){
    
}

